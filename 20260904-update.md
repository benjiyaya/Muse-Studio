# 2026-09-04 Update

## Extensions + Ask Muse Consolidation

### Completed
- Consolidated Ask Muse capabilities into Extensions chat flow.
- Added built-in Muse tool capabilities for orchestration:
  - `muse.story`
  - `muse.visual`
  - `muse.motion`
- Kept MCP tools panel MCP-only (built-in Muse tools are not listed there).
- Redirected `/ask-muse` to `/mcp-extensions` with query compatibility support.

### Session & Context
- Added multi-session support enhancements:
  - Create, switch, rename, delete, pin, search sessions.
- Added session context persistence:
  - `project_id` and `scene_id` per chat session.
- Added empty-session start UX:
  - Select project and start, or continue as general chat.
- Added active project context chip in chat area.

### Attachments & Upload Flow
- Removed manual upload target selector in composer.
- Upload target is now orchestrated from session context:
  - Project context -> upload to project
  - No project context -> upload to session
- Improved attachment reliability and ID fallback behavior in non-secure contexts.

### Built-in Muse Tool Execution
- Implemented execution paths for built-in Muse tools in unified executor.
- Added session context forwarding (`projectId` / `sceneId`) through chat API.
- Fixed SSE chunk handling for built-in Muse responses (string and binary chunk compatibility).

### UI Improvements
- Removed top header Ask Muse button (Extensions is now primary entrypoint).
- Updated shortcut behavior to route to `/mcp-extensions`.
- Added stronger hover/focus affordances on output action buttons:
  - Assign to keyframe
  - Assign to character
  - Assign video to scene
- Updated empty-state onboarding actions to avoid user confusion after project selection.

### Stability / Fixes
- Added resilient DB/session schema checks to avoid missing table migration issues.
- Resolved runtime issue: `crypto.randomUUID is not a function` fallback path in client IDs.
- Addressed strict MCP schema argument mismatch by preventing extra injected fields for strict MCP calls.

## Notes
- Extensions orchestration currently uses LLM JSON planning + tool execution.
- LangGraph is used in other backend flows (e.g. suggestions), not the Extensions chat orchestration path.

## Verification
- Lint checks passed on touched files.
- No new linter diagnostics reported in updated modules.